const NavElement = ({ name, to }) => {
	return (
		<p className="text-white font-bold text-lg text-transparent bg-clip-text bg-gradient-to-bl from-rose-400 to-rose-600">
			{name}
		</p>
	);
};

export default NavElement;
